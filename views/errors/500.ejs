<%- include('./_shell', {
  status: 500,
  heading: "Server error",
  body: typeof message !== 'undefined' && message && 500 < 500 ? message : "Something went wrong on our side. The issue has been logged and will be looked into.",
  stack: typeof stack !== 'undefined' ? stack : null,
  isAdminArea: typeof isAdminArea !== 'undefined' ? isAdminArea : false,
}) %>
